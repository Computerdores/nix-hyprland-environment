(defwidget float_label [value ?unit ?class ?hexpand ?halign ?visible]
  (label :text "${round(value,0)}${unit ?: ''}"
         :class {class ?: ""}
         :hexpand {hexpand ?: false}
         :halign {halign ?: "start"}
         :visible {visible ?: true}))
